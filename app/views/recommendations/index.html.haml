- provide(:title, "Eatability of #{@recipe[:name].titleize}")
= link_to "Back", new_recommendation_path
- unless is_plural?(@recipe[:name])
  - letter = "s"
%h1 #{@recipe[:name].titleize} get#{letter} an eatability rating of #{@recipe[:average_rating]}
%h2= @recipe
%p
  Poor
  %input{disabled: "disabled", max: "50", min: "0", name: "points", type: "range", value: @recipe[:adjusted_rating]}
  Good